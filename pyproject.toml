[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "URI_2022"
authors = [{name = "Oscar Centro", email = "oscar.centro@corteconti.it"}]
dynamic = ["version", "description"]
